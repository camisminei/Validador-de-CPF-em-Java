a) Interface Tributavel
 Método:
a) double calcularImpostos();
Este método será implementado nas classes filhas de Funcionário. O método retornar o
valor do (INSS + IR) que será descontado do salário do funcionário, de acordo com o
valor recebido no mês.
(b) Interface Verificavel
 Métodos:
a) boolean validarCPF(String CPF);
b) void solicitarNovoCPF();
(c) Classe Pessoa (Abstrata)
 Implementa a classe Verificavel
 Atributos privados:
String nome; String cpf; String sexo; String celular; String email;
 1 Construtor
a) public Pessoa(String nome, String cpf, String sexo, String celular, String email)
obs: no construtor deve-se validar o CPF antes de atribuir o valor do cpf
Então é preciso chamar o método para validar CPF
Se o CPF estiver correto, ele será atribuído,
Caso contrário deverá solicitar um novo valor para o cpf
 Métodos:
a) @Override
public boolean validarCPF(String CPF)
este método terá que validar o CPF
b) @Override
public void solicitarNovoCPF()
este método solicita um novo CPF, até que o valor do CPF informado esteja correto
(d) Classe Endereço
 Atributos:
String rua; String numero; String complemento; String bairro;
String cidade; String cep;
 2 construtores:
a) Para endereço completo
public Endereco(String rua, String numero, String complemento, String bairro, String
cidade, String cep)
b) Para endereço sem complemento
public Endereco(String rua, String numero, String bairro, String cidade, String cep)
 Métodos:
a) gets e sets
b) public String toString()
retorna uma String com o endereço
(e) Classe Funcionario (Abstrata)
 Estende a classe Pessoa
 Implementa a classe Tributável
 Tem um objeto do tipo Endereco
 1 construtor com os parâmetros de pessoa e o Endereco
 Métodos:
a) public abstract double calcularSalario();
(f) Classe FuncionarioMensalista
 Estende a classe Funcionario
 Atributo: private double salarioMensal
 1 construtor com os parâmetros de Funcionário e o salarioMensal
 Métodos:
a)gets e sets
b)@Override
calcularSalário()
Retorna o valor do getSalarioMensal() 􀀀 calcularImposto();
c) @Override
double calcularImpostos();
O método retornar o valor do (INSS + IR) que será descontado do salário do funcionário,
de acordo com o valor recebido no mês.
d) @Override
public String toString()
retorna uma String com o nome do funcionário, o endereço e o salário que ele recebeu
(g) Classe FuncionarioHorista
 Estende a classe Funcionario
 Atributos:
private double salarioHora //valor do salário por hora
private double horas //total de horas trabalhadas no mês
 1 construtor com os parâmetros de Funcionário e o valor do salarioHora que o funcionário
irá receber. O total de horas trabalhadas no mês inicialmente será 0.
 Métodos:
a)gets e sets
b)@Override
calcularSalário()
Retorna o valor do (getSalarioHora()  getHoras()) 􀀀 calcularImposto();
c) @Override
double calcularImpostos();
O método retornar o valor do (INSS + IR) que será descontado do salário do funcionário,
de acordo com o valor recebido no mês.
d)@Override
public String toString()
retorna uma String com o nome do funcionário, o endereço e o salário que ele recebeu
Classe FuncionarioComissionado
 Estende a classe Funcionario
 Atributos:
private double vendasBrutas //valor total das vendas efetuadas no mês
private double taxaComissao
 1 construtor com os parâmetros de Funcionário e a taxaCamissao (porcentagem da comissão)
combinada no contrato. O valor das vendasBrutas no mês, inicialmente será
zero.
 Métodos:
a) gets e sets
b) @Override
calcularSalário() Retorna o valor: (getTaxaComissao()getV endasBrutas())􀀀calcularImposto()c) @Override
double calcularImpostos();
O método retornar o valor do (INSS + IR) que será descontado do salário do funcionário,
de acordo com o valor recebido no mês.
d)@Override
public String toString()
retorna uma String com o nome do funcionário, o endereço e o salário que ele recebeu